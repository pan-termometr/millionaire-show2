version: "2"
services:
  millionaire:
    build:
      context: ./
      dockerfile: Dockerfile
    restart: always
    ports:
      - "80"
    networks:
      piotrzur_net:
        - aliases:
            - millionaire
networks:
  piotrzur_net:
    external: true
